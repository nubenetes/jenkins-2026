apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: petclinic
  labels:
    app: billing
spec:
  selector:
    app: billing
  ports:
    - name: http
      port: 80
      targetPort: 8082
