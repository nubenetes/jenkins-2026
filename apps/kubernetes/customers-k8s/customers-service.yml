apiVersion: v1
kind: Service
metadata:
  name: customers
  namespace: petclinic
  labels:
    app: customers
spec:
  selector:
    app: customers
  ports:
    - name: http
      port: 80
      targetPort: 8081
