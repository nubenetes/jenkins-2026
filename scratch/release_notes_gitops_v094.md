# GitOps Target State Release: v0.9.4

This release aligns the GitOps target state configurations with the **v0.9.4** infrastructure stack release, maintaining alignment across all orchestration and pipeline environments.

---

## 🚀 Key Updates in v0.9.4
* **Tag and Version Alignment**: Published matching `v0.9.4` release tags to guarantee compatibility during lifecycle workflows and Jenkins pipeline checks.
* **Stable Configuration Mapping**: Anchored target state validations to the stable release branch.
