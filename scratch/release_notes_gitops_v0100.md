# GitOps Target State Release: v0.10.0

This release aligns the GitOps target state configurations with the **v0.10.0** infrastructure stack release.

---

## 🚀 Key Updates in v0.10.0
*   **Tag and Version Alignment**: Published matching `v0.10.0` release tags to guarantee compatibility during lifecycle workflows and Jenkins pipeline checks.
*   **Authentication Stability**: Integrates the latest ArgoCD auth token generation logic to ensure consistent deployment states across GitOps managed microservices.
