# Tekton Triggers (controller + webhook) and the core interceptors, pulled as
# pinned upstream release kustomize remote resources.
#
# Pinned version - keep in sync with config/config.yaml ci.tekton.versions.triggers.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://storage.googleapis.com/tekton-releases/triggers/previous/v0.31.0/release.yaml
  - https://storage.googleapis.com/tekton-releases/triggers/previous/v0.31.0/interceptors.yaml
