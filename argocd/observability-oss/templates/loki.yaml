apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oss-loki
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: {{ .Values.charts.loki.repoURL | quote }}
      chart: loki
      targetRevision: {{ .Values.charts.loki.version | quote }}
      helm:
        valueFiles:
          - $values/observability/grafana/values-oss-loki.yaml
    - repoURL: {{ .Values.repoURL | quote }}
      targetRevision: {{ .Values.targetRevision | quote }}
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.namespace.observability | quote }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
